services:
  pipeline:
    build: .
    image: atomic-press
    entrypoint: /app/scripts/build.sh
    volumes:
      - ./app:/app:ro
      - ${VAULT_PATH}:/vault:ro
      - ./build:/build
    working_dir: /build