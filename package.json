{
	"name": "nope",
	"version": "0.4.1",
	"description": "Export Obsidian notes to publication-ready PDFs via a Dockerized Pandoc + LaTeX (Eisvogel) toolchain.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"bundle-assets": "node scripts/bundle-assets.mjs",
		"dev": "npm run bundle-assets && node esbuild.config.mjs",
		"build": "npm run bundle-assets && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "eslint ."
	},
	"keywords": [],
	"license": "GPL-3.0-or-later",
	"devDependencies": {
		"@eslint/js": "^9.30.1",
		"esbuild": "0.25.5",
		"eslint": "^9.30.0",
		"eslint-plugin-obsidianmd": "^0.4.0",
		"globals": "14.0.0",
		"jiti": "2.6.1",
		"tslib": "2.4.0",
		"typescript": "^5.8.3",
		"typescript-eslint": "^8.35.1"
	},
	"dependencies": {
		"obsidian": "latest",
		"pdfjs-dist": "~4.7.76"
	},
	"overrides": {
		"tar": "^7.5.16"
	}
}
